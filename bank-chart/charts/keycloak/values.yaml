replicaCount: 1
image:
  repository: quay.io/keycloak/keycloak
  tag: 26.1.3
  pullPolicy: IfNotPresent

service:
  port: 8090

resources:
  requests:
    memory: "512Mi"
    cpu: "250m"
  limits:
    memory: "1Gi"
    cpu: "500m"

postgresql:
  enabled: true