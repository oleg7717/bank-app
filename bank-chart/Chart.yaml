apiVersion: v2
name: bank-chart
description: umbrella Helm chart for Bank Microservices

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"

dependencies:
  - name: postgresql-statefulset
    version: ">=1.0.0"
    repository: "file://charts/postgresql-statefulset"
    condition: postgresql.enabled
  - name: redis
    version: ">=1.0.0"
    repository: "file://charts/redis"
    condition: redis.enabled
  - name: keycloak
    version: ">=1.0.0"
    repository: "file://charts/keycloak"
    condition: keycloak.enabled
  - name: account-service
    version: ">=1.0.0"
    repository: "file://charts/account-service"
    condition: account_service.enabled
  - name: cash-service
    version: ">=1.0.0"
    repository: "file://charts/cash-service"
    condition: cash_service.enabled
  - name: transfer-service
    version: ">=1.0.0"
    repository: "file://charts/transfer-service"
    condition: transfer_service.enabled
  - name: notification-service
    version: ">=1.0.0"
    repository: "file://charts/notification-service"
    condition: notification_service.enabled
  - name: antifraud-service
    version: ">=1.0.0"
    repository: "file://charts/antifraud-service"
    condition: antifraud_service.enabled
  - name: exchange-service
    version: ">=1.0.0"
    repository: "file://charts/exchange-service"
    condition: exchange_service.enabled
  - name: exchange-generator
    version: ">=1.0.0"
    repository: "file://charts/exchange-generator"
    condition: exchange_generator.enabled
  - name: bank-front
    version: ">=1.0.0"
    repository: "file://charts/bank-front"
    condition: bank_front.enabled