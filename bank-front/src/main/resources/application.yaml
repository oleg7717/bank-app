spring:
  application:
    name: bank-front
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8090/realms/master
        registration:
          bank-front-service: # Описание клиента OAuth2
            provider: keycloak
            authorizationGrantType: authorization_code
            client-id: bank-front-service
            client-secret: Lwdnla3EWnF4Rxqd39S3XxALq1UEzxs0
            scope: openid, profile, email
            redirect-uri: "http://localhost:8080/login/oauth2/code/{registrationId}"
#  datasource:
#    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=bank-front
#    username: postgres
#    password: password
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: none
#  sql:
#    init:
#      mode: always

server:
  port: 8080

application:
  service:
    account:
      url: http://localhost:8081
    cash:
      url: http://localhost:8082
    trsnsfer:
      url: http://localhost:8083
    postLogoutRedirectUri: http://localhost:8080/account